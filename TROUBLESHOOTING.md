# 故障排查

## API Key 无效或 401

- 应用会自动识别平台；若无法识别，请确认粘贴的是完整真实 Key，而不是示例或星号遮罩。
- 自动识别只调用各平台的模型列表接口，不会上传 PDF 或问题；公司网络、代理、地区/IP 白名单也可能阻止验证。
- 确认 API 账户有余额。ChatGPT 订阅与 OpenAI API 账单相互独立。
- 在服务商后台检查模型权限、地区/IP 白名单和 Key 状态，然后回到设置点击“刷新模型”。
- 如果密钥曾出现在公开位置，应立即撤销并创建新 Key。

## 目录识别不准

- 先确认 PDF 是否自带目录；自带目录可信时应用会优先使用。
- 扫描件请等待 OCR 完成。双栏、竖排或复杂版面会自动使用安装包内置的增强目录识别，无需另行下载。
- 若设置中显示“组件不完整”，请删除当前应用并重新从完整 DMG 安装；不要只复制可执行文件。
- 手动添加时每行只放一条目录，并保留标题和印刷页码；斜杠、点线和空格可由解析器清理。
- 写入 Obsidian 前，抽查第一条、正文第一章和最后一条的跳转。

## 找不到 Obsidian Vault

- 先在 Obsidian 中使用“打开文件夹作为仓库”打开该文件夹。
- 回到应用重新选择 Vault 的根目录，不要选择 Vault 内的 `Reading Companion Open` 子目录。
- 如果使用 `obsidian://` 无法打开，请确认 Obsidian 已安装并至少启动过一次。

## 语音按钮无响应

打开“系统设置 > 隐私与安全性”，分别允许 Reading Companion Open 使用“麦克风”和“语音识别”，然后重启应用。

## macOS 阻止首次运行

当前社区构建采用临时签名，未经过 Apple 公证。在 Finder 中右键应用并选择“打开”；只从可信的项目发布页下载安装包，并可核对发布页提供的 SHA-256。
